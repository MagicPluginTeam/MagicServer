const express = require("express")

let router = express.Router()

router
    .post("/signin", (req, res) => {
        
    })
    .post("/signup", (req, res) => {
        
    })

module.exports = router